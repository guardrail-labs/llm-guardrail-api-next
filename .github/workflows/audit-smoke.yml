name: audit-smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Audit smoke placeholder
        run: |
          echo "audit-smoke workflow disabled (placeholder)."
