name: Nightly Audit Smoke

on:
  schedule:
    - cron: "0 7 * * *"   # daily at 07:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prod-smoke:
    # Only runs if you explicitly set AUDIT_SMOKE_ENABLED=1 in repo secrets.
    if: ${{ secrets.AUDIT_SMOKE_ENABLED == '1' }}
    runs-on: ubuntu-latest
    steps:
      - name: Audit nightly placeholder
        run: |
          echo "Nightly audit smoke disabled (placeholder)."
          echo "Set AUDIT_SMOKE_ENABLED=1 to enable."
