name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      PIP_NO_CACHE_DIR: "1"
      PIP_DEFAULT_TIMEOUT: "60"
      PIP_INDEX_URL: https://pypi.org/simple
      # Test-only env to align with your suite
      GUARDRAIL_DISABLE_AUTH: "1"   # bypass admin auth in tests
      CORS_ALLOW_ORIGINS: ""
      PYTHONPATH: .
      VERIFIER_TIMEOUT_MS: "2500"
      VERIFIER_MAX_TOKENS_PER_REQUEST: "800"
      VERIFIER_DAILY_TOKEN_BUDGET: "100000"
      VERIFIER_CIRCUIT_FAILS: "5"
      VERIFIER_CIRCUIT_WINDOW_S: "300"
      VERIFIER_CIRCUIT_COOLDOWN_S: "600"
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          check-latest: true

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt', 'pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install project
        run: |
          python -V
          python -m pip install -U pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f pyproject.toml ]; then pip install -e .; fi

      - name: Install dev tools
        run: |
          python -m pip install ruff mypy pytest

      - name: Lint (ruff)
        run: |
          ruff check --fix .

      - name: Typecheck (mypy)
        run: |
          # Auto-install missing type stubs non-interactively
          mypy --install-types --non-interactive .

      - name: Tests (pytest)
        run: |
          pytest -q
