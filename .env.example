APP_ENV=dev
APP_NAME=llm-guardrail-api-next
LOG_LEVEL=INFO
PORT=8080

# Policy / Verifier toggles
VERIFIER_MODE=never

# --- Security ---
# Provide either a single API_KEY, or a comma-separated list in API_KEYS
# For production, set these as GitHub Actions/host secrets (do NOT commit a real key).
API_KEY=
# API_KEYS=key1,key2,key3

# Example external keys (optional)
OPENAI_API_KEY=
