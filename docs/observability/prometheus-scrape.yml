global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'guardrail-api'
    metrics_path: /metrics
    static_configs:
      - targets:
          - '127.0.0.1:8000' # dev override; replace with your svc DNS in prod
