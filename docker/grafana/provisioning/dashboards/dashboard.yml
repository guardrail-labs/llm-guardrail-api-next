apiVersion: 1
providers:
  - name: "Guardrail Starter"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      # Use the dashboard that lives in the repo
      paths:
        - /repo/observability/grafana

