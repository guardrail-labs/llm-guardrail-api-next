apiVersion: 1
providers:
  - name: Guardrail Core
    orgId: 1
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      # Point to the compose-mounted repo path that contains the dashboard JSONs
      path: /repo/observability/grafana
